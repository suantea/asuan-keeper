# asuansync (asuan-keeper)

数据同步工具:本质是"迷你 OneDrive/Seafile 混合体"——占位符按需拉取 + 纯 P2P 局域网同步,NAS 作常驻中转 hub(公开表述为"文件缓存与分发工具",实为数据同步)。

## 项目规则

- 语言:Go(见根目录 `go.mod`);客户端 Windows/macOS 绿色便携,NAS 端 Docker 部署
- 目录结构:程序目录放 `asuan.exe` + `syncthing.exe`,同目录建 `asuan.json`(`asuan init` 生成)
- 同步引擎基于 Syncthing,`asuan.json` 中 `folders[].id` 各端一致、`policy` 为 `sync`
- 网页控制台默认 `http://127.0.0.1:18084`(hub 为 `http://<NAS>:18084`)
- 进度与历史见 `PROGRESS.md`、`_legacy_history/`
